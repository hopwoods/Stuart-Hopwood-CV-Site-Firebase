import { createTheme } from "@material-ui/core/styles";

export const materialTheme = createTheme({
  palette: {
    primary: {
      main: "#B40542",
    },
    secondary: {
      main: "#ababab",
    },
  },
});
