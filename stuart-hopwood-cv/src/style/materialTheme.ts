import { createTheme } from "@mui/material";


export const materialTheme = createTheme({
  palette: {
    primary: {
      main: "#FD0151",
    },
    secondary: {
      main: "#FFFFFF",
    },
  },
});
